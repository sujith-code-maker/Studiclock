name: Build StudiClock APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Create Cordova Project and Build
        run: |
          npm install -g cordova
          cordova create app com.studiclock.app StudiClock
          cd app
          cordova platform add android@10.1.2
          cordova build android --debug
          mkdir -p ../output
          cp platforms/android/app/build/outputs/apk/debug/app-debug.apk ../output/StudiClock.apk

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v3
        with:
          name: StudiClock-APK
          path: output/StudiClock.apk
